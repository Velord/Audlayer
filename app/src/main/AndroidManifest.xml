<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="velord.university">

    <permission android:name="velord.university.PERM_PRIVATE_MINI_PLAYER"
        android:protectionLevel="signature"/>

    <uses-permission android:name = "android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name = "android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission
        android:name="velord.university.PERM_PRIVATE_MINI_PLAYER"/>

    <application
        android:name=".application.AudlayerApp"

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name="velord.university.application.service.MiniPlayerService"
            android:enabled="true"
            android:exported="true">
        </service>

        <service android:name="velord.university.application.service.MiniPlayerServiceBroadcastReceiver"
            android:enabled="true"
            android:exported="true">

        </service>

        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>